<div class="card">
  <div class="flex-row justify-space-between card-header">
    <h3 class="card-header">{{title}}</h3>
    <h5 class="card-header"> Posted by {{user.username}} on {{format_date created_at}} |
      {{comments.length}} comment(s)</h5>
  </div>
  <p class="card-body">{{content}}</p>
</div>