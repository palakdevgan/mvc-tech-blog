  <div class="card">
        <h3 class="card-header">{{title}}
         Posted by {{user.username}} on {{format_date created_at}}  |
        <a class="navitems" href="/post/{{id}}">{{comments.length}} comment(s)</a>
        </h3>
        <p class="card-body">{{content}}</p>
    </div>